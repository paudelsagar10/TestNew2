name: Installing npm

on: 
  push:
    branches:
      - main

jobs:
  build:
    runs-on: 
      - ubuntu-latest

      
    steps:
    # - name: Set Up Node.js
    #   uses: actions/setup-node@v3
    #   with: 
    #     node-version: '16'

    - name: Installing npm
      run: |
        - pwd
        - ls

    - name: Starting npm
      run: npm start     